interface:
  display_name: "JD Shopping"
  short_description: "京东可见浏览器的低频登录、搜索、比选和加购辅助（不结算、不付款）"
  default_prompt: "Use $jd-shopping to log in manually, search JD products, and add an approved item to cart without checkout."
